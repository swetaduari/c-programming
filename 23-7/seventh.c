#include<conio.h>
#include<stdio.h>
 
int main()
{
    int a;
    printf("enter the number:");
    scanf("%d",& a);
    int sqrt= a*a;
    int cube= a*a*a;
    printf("the sqaure root of number is %d",sqrt);
    printf("the cube root of number is %d",cube);
}